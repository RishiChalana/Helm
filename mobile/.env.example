# ── Backend API ───────────────────────────────────────────────────────────────
# Inlined by Metro at JS bundle time — change requires a full rebuild (expo run:android).
# Local development (Android emulator via ADB reverse):
#   EXPO_PUBLIC_API_URL=http://10.0.2.2:8000/api/v1
# Deployed backend:
EXPO_PUBLIC_API_URL=https://your-render-app.onrender.com/api/v1
